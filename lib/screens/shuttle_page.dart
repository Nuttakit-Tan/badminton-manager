import 'package:flutter/material.dart';

class ShuttlePage extends StatelessWidget {
  const ShuttlePage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("หน้าเพิ่มรายการลูกแบด", style: TextStyle(fontSize: 24)),
    );
  }
}
