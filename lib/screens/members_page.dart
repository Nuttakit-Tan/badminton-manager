import 'package:flutter/material.dart';

class MembersPage extends StatelessWidget {
  const MembersPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("หน้าสร้างรายชื่อ", style: TextStyle(fontSize: 24)),
    );
  }
}
