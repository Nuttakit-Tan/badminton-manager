import 'package:flutter/material.dart';

class HistoryPage extends StatelessWidget {
  const HistoryPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("ประวัติการจัดก๊วน", style: TextStyle(fontSize: 24)),
    );
  }
}
